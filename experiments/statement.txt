user query if is with link 
   - link -> extract link and query 
   - splitting of the doc
   - embedding -> vectordb
   - retrieval
   - query and retrieved context -> response

if not use tool of duckduckgo to response

